name: handoff-polish
description: Hands Markdown copyediting off to the Antigravity CLI (agy) and verifies the outcome from file hashes

targets:
  - claude
  - codex
  # - antigravity   # agy runs polish-doc directly; handing off to itself is a loop
