name: handoff-commit-safe
description: Hands the git-commit workflow off to the Antigravity CLI (agy) with the whitelist and timestamp resolved locally

# Documentation only. The runner lives in the handoff-commit skill and is invoked
# with --safe; the checks live in the git-commit-safe CLI. Both are installed by
# this module, so nothing extra is needed here.

targets:
  - claude
  - codex
  # - antigravity   # agy runs git-commit directly; handing off to itself is a loop
